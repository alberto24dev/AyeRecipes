# 🎯 Diagrama Visual: Ciclo de Vida de CHHapticEngine

## 📊 El Problema Visualizado

### ❌ Ciclo de Vida INCORRECTO (Múltiples Instancias)

```
┌─────────────────────────────────────────────────────────────────────┐
│                         APP INICIALIZANDO                            │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────────┐
                    │   Usuario presente  │
                    │   Botón disponible  │
                    └────────────┬────────┘
                                 │
                  ┌──────────────┬──────────────┐
                  │                             │
                  ▼                             ▼
        ┌──────────────────────┐    ┌──────────────────────┐
        │   Usuario tapa 1     │    │   Usuario tapa 2     │
        │                      │    │   (0.5 segundos)     │
        └──────────┬───────────┘    └──────────┬───────────┘
                   │                           │
                   ▼                           ▼
        ┌──────────────────────┐    ┌──────────────────────┐
        │ CHHapticEngine()     │    │ CHHapticEngine()     │
        │ MOTOR 1 creado       │    │ MOTOR 2 creado       │
        │ ← En memoria         │    │ ← En memoria (nuevo) │
        └──────────┬───────────┘    └──────────┬───────────┘
                   │                           │
                   ▼                           ▼
        ┌──────────────────────┐    ┌──────────────────────┐
        │ engine.start()       │    │ MOTOR 1 se destruye  │
        │ Conectado a XPC ✅   │    │ (scope termina)      │
        └──────────┬───────────┘    │ deinit llama a       │
                   │                │ close() en XPC       │
                   ▼                │                      │
        ┌──────────────────────┐    │ 💥 Service invalid   │
        │ Feedback en proceso  │    │                      │
        │ ✅ Éxito            │    └──────────┬───────────┘
        └──────────┬───────────┘                │
                   │                           ▼
                   │              ┌──────────────────────────┐
                   │              │ MOTOR 2 intenta start()  │
                   │              │ Pero el servicio XPC:    │
                   │              │ ❌ ESTÁ DESCONECTADO    │
                   │              │ ❌ connection was        │
                   │              │    invalidated           │
                   │              │                          │
                   │              │ 💥 CRASH                │
                   │              └──────────────────────────┘
                   │
                   └──→ RESULTADO: Inconsistente, crashes aleatorios
```

---

### ✅ Ciclo de Vida CORRECTO (Singleton)

```
┌────────────────────────────────────────────────────────────────────────────┐
│                          APP INICIALIZANDO                                  │
└──────────────────────────────────┬─────────────────────────────────────────┘
                                   │
                                   ▼
                        ┌────────────────────────┐
                        │  HapticManager.shared  │
                        │  Singleton inicializado│
                        └────────────┬───────────┘
                                     │
                                     ▼
                        ┌────────────────────────────────────┐
                        │ prepareHapticEngine() se ejecuta   │
                        │ UNA SOLA VEZ                       │
                        └────────────┬───────────────────────┘
                                     │
                   ┌─────────────────┼─────────────────┐
                   │                 │                 │
                   ▼                 ▼                 ▼
        ┌──────────────────┐ ┌──────────────┐ ┌────────────────┐
        │ Verificar        │ │ Crear        │ │ Configurar     │
        │ hardware soporta │ │ CHHapticEngine│ │ callbacks      │
        │ haptics          │ │              │ │ (reset handler)│
        │                  │ │ MOTOR ÚNICO  │ │                │
        │ ✅ true         │ │ (permanente) │ │ Auto-recovery  │
        └──────────┬───────┘ └──────────┬───┘ └────────┬───────┘
                   │                    │               │
                   └────────────────────┼───────────────┘
                                        │
                                        ▼
                        ┌────────────────────────────────────┐
                        │ HapticManager listo                │
                        │ Motor vivo y esperando órdenes     │
                        │ isAvailable = true                 │
                        │ isEngineRunning = false (standby)  │
                        └────────────┬───────────────────────┘
                                     │
              ┌──────────────────────┬───────────┬──────────────┐
              │                      │           │              │
              ▼                      ▼           ▼              ▼
    ┌──────────────────┐  ┌──────────────┐  ┌──────────┐  ┌────────────┐
    │ Usuario tapa 1   │  │ Usuario tapa │  │ Usuario  │  │ App cierra │
    │ (t=0ms)          │  │ tapa 2       │  │ tapa N   │  │            │
    │                  │  │ (t=500ms)    │  │ (t=XXXms)│  │            │
    └────────┬─────────┘  └────────┬─────┘  └────┬─────┘  └────────┬───┘
             │                     │              │                 │
             ▼                     ▼              ▼                 ▼
    ┌──────────────────────────────────────────────────┐  ┌─────────────────┐
    │ HapticManager.playSimpleFeedback()               │  │ Limpiar recursos│
    │ (puede llamarse múltiples veces - IDEMPOTENTE)  │  │ engine.stop()   │
    └────────────┬─────────────────────────────────────┘  └────────┬────────┘
                 │                                                 │
                 ▼                                                 ▼
    ┌──────────────────────────────────────────────────┐  ┌──────────────┐
    │ playSimpleFeedback() {                           │  │ Conexión XPC │
    │   guard !isEngineRunning { return }             │  │ cerrada      │
    │   startEngine()  // ✅ Idempotente              │  │ correctamente │
    │   engine?.start() // ✅ Si necesario            │  │              │
    │   // Reproducir feedback                        │  │ ✅ Clean exit│
    │   Feedback háptico ✅ ÉXITO                     │  └──────────────┘
    │ }                                                │
    └──────────────────────────────────────────────────┘
             │      │      │
             ▼      ▼      ▼
    ┌──────────────────────────────────────────────────┐
    │ El MISMO motor se reutiliza                      │
    │ No hay creación de nuevas instancias             │
    │ No hay desconexiones de XPC                      │
    │ ✅ Cero crashes                                  │
    │ ✅ Comportamiento predecible                     │
    └──────────────────────────────────────────────────┘

    RESULTADO: Comportamiento consistente y confiable
```

---

## 🔄 Flujo de Control Detallado

### Inicialización (Una sola vez)

```
HapticManager.shared
       │
       ▼
┌─────────────────────────────────────────┐
│ private init() {                        │
│   prepareHapticEngine()                 │
│ }                                       │
└──────────────┬────────────────────────┘
               │
               ▼
    ┌────────────────────────────┐
    │ prepareHapticEngine() {    │
    │                            │
    │ 1. guard supportsHaptics   │──→ isAvailable = true/false
    │ 2. engine = CHHapticEngine()
    │ 3. setupEngineCallbacks()  │──→ resetHandler configurado
    │ }                          │
    └────────────────────────────┘
```

### Durante Uso (Múltiples veces)

```
HapticManager.shared.playSimpleFeedback()
       │
       ▼
┌──────────────────────────────┐
│ dispatch a hapticQueue {     │
│   Task @MainActor {          │
│     startEngine()            │
│   }                          │
│ }                            │
└──────────────┬────────────────┘
               │
               ▼
        ┌─────────────────┐
        │ startEngine() { │
        │ guard           │──→ ✅ Idempotente
        │ !running        │
        │ else { return } │
        │ engine.start()  │
        │ }               │
        └─────────────────┘
               │
               ▼
    ┌────────────────────────────┐
    │ generateFeedback()         │
    │ • Crear patrón háptico     │
    │ • Crear player             │
    │ • Reproducir               │
    │ ✅ Motor reutilizado      │
    │ ✅ Sin crear nuevo motor   │
    └────────────────────────────┘
```

### Manejo de Desconexión (Auto-Recovery)

```
Servicio XPC falla (inesperado)
       │
       ▼
┌──────────────────────────────────────────┐
│ engine.resetHandler se ejecuta           │
│ (callback automático de CHHapticEngine)  │
└──────────────┬───────────────────────────┘
               │
               ▼
        ┌──────────────────┐
        │ reconnectEngine()│
        │ {               │
        │ stopEngine()    │
        │ delay(0.1s)     │
        │ startEngine()   │
        │ }               │
        └────────────┬────┘
                     │
                     ▼
        ┌──────────────────────────┐
        │ Motor reconectado ✅    │
        │ No requiere código manual│
        │ No requiere UI change   │
        │ No requiere restart     │
        └──────────────────────────┘
```

### Limpieza (Al cerrar app)

```
App terminating
       │
       ▼
  HapticManager deinit
       │
       ▼
┌────────────────────┐
│ stopEngine() {     │
│ engine.stop()      │
│ isEngineRunning=false
│ }                  │
└────────┬───────────┘
         │
         ▼
   XPC connection
   closed properly
   (no abrupt termination)
```

---

## 🎛️ Control de Estado

```
┌─────────────────────────────────────────────────────────────┐
│                    HapticManager State                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ isAvailable: Bool                                          │
│ ├─ true  → Hardware soporta haptics                       │
│ └─ false → Hardware no soporta / error de inicialización  │
│                                                             │
│ isEngineRunning: Bool                                      │
│ ├─ true  → Motor iniciado y listo para reproducir         │
│ └─ false → Motor detenido (para ahorrar poder)           │
│                                                             │
│ engine: CHHapticEngine?                                    │
│ ├─ nil   → Error durante inicialización                  │
│ └─ Valid → Motor listo para usar                         │
│                                                             │
│ Flujo seguro:                                              │
│ if isAvailable {                                            │
│   startEngine()        // Verifica isEngineRunning        │
│   playFeedback()       // Reproduce si todo está OK       │
│ }                                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚦 Máquina de Estados

```
                         ┌─────────────────────┐
                         │    NOT_AVAILABLE    │
                         │ (hardware no soporta)
                         └─────────────────────┘
                                   ▲
                                   │ error
                                   │
                         ┌─────────┴──────────┐
                         │                    │
                         ▼                    ▼
                 ┌──────────────────────────────────┐
                 │   Engine Created & Ready        │
                 │   (isAvailable = true)          │
                 │   (isEngineRunning = false)     │
                 └──────────┬───────────────────────┘
                            │
                 call playSimpleFeedback()
                            │
                            ▼
                 ┌──────────────────────────┐
                 │  Engine Running          │
                 │  (isEngineRunning=true)  │
                 └──────────┬────────────────┘
                            │
                        feedback
                        complete
                            │
                            ▼
        (opcional)  ┌─────────────────────────┐
        ────────→ │ Engine Stopped           │
                  │ (isEngineRunning=false)  │
                  │ (conserva energía)       │
                  └──────────┬────────────────┘
                             │
                    startEngine() llamado
                             │
                             ▼
              (vuelve a Engine Running)

    En caso de desconexión XPC:
    
    Engine Running
             │
    resetHandler()
             │
             ▼
    reconnectEngine()
             │
             ▼
    Engine Stopped
             │
             ▼ delay(0.1s)
    startEngine()
             │
             ▼
    Engine Running ✅
```

---

## 📊 Comparativa de Memoria

### ❌ Sin HapticManager (Múltiples instancias)

```
Tiempo →

t=0ms   │ Engine1 creado (ref count: 1)
        │ └─ Vivo en memoria
t=50ms  │ Engine2 creado (ref count: 1)
        │ ├─ Vivo en memoria
        │ └─ Engine1: ref count → 0 (se destruye)
        │    └─ XPC connection interrupted 💥
t=100ms │ Engine3 creado
        │ ├─ Vivo en memoria
        │ └─ Engine2: ref count → 0 (se destruye)
        │    └─ XPC connection interrupted 💥
        │
        └─ RESULTADO: Inestable, muchos crashes
```

### ✅ Con HapticManager (Una sola instancia)

```
Tiempo →

t=0ms   │ HapticManager singleton creado
        │ └─ Engine creado (ref count: permanentemente > 0)
        │    └─ Vivo en memoria hasta que se cierre la app
t=50ms  │ playSimpleFeedback() #1
        │ └─ Reutiliza el mismo engine
t=100ms │ playSimpleFeedback() #2
        │ └─ Reutiliza el mismo engine
t=150ms │ playSimpleFeedback() #3
        │ └─ Reutiliza el mismo engine
        │
t=app   │ App cierra
close   │ HapticManager deinit
        │ └─ engine.stop() (limpieza correcta)
        │    └─ XPC connection closed properly ✅
        │
        └─ RESULTADO: Estable, sin crashes
```

---

## 🔐 Thread Safety Guarantee

```
┌────────────────────────────────────────────────────┐
│ HapticManager Guarantees:                          │
├────────────────────────────────────────────────────┤
│                                                    │
│ @MainActor                                        │
│ ├─ Todas las operaciones en Main thread           │
│ ├─ UI updates automáticamente en MainThread       │
│ └─ Evita race conditions con UIKit/SwiftUI       │
│                                                    │
│ hapticQueue (DispatchQueue)                       │
│ ├─ Cola serializada (una tarea a la vez)         │
│ ├─ Previene múltiples startEngine() simultáneos  │
│ └─ Previene race conditions en inicialización    │
│                                                    │
│ Resultado:                                         │
│ ✅ Thread-safe desde cualquier View               │
│ ✅ Sin locks o semaphores explícitos             │
│ ✅ Permite múltiples llamadas simultáneas         │
│                                                    │
└────────────────────────────────────────────────────┘
```

---

## 🎓 Resumen Visual

| Aspecto | Sin Manager | Con Manager |
|---------|-----------|-----------|
| **Instancias de Engine** | ❌ Múltiples | ✅ Una única |
| **Ciclo de Vida** | ❌ Incontrolable | ✅ Seguro |
| **Crashes XPC** | ❌ Frecuentes | ✅ Ninguno |
| **Thread Safety** | ❌ Race conditions | ✅ Garantizado |
| **Reconexión** | ❌ Manual | ✅ Automática |
| **Complejidad** | ❌ Alta en views | ✅ Baja (1 llamada) |
| **Debugging** | ❌ Difícil | ✅ Logging completo |

